# PickNext 用户流程评审记录

## 第一轮结论

- 唱歌热爱者：建库、Pick、唱完记录、长期推荐闭环可以跑通；主要问题是首次建库入口偏重。
- 唱歌小白：技术路径可以跑通，但空曲库和“会唱/待学”概念引导不足，容易卡在第一次 Pick 前。
- 经常去 KTV：准备歌单、优先 Pick、唱完自动移除的闭环可以跑通，是当前最完整的场景。
- 唱歌大佬：曲库管理、个人难度、Key、备注和高级筛选基本可以跑通；批量管理和 Pick 的语言/曲风筛选不足。

## 后续优先问题

1. 首次使用时根据曲库状态提供明确下一步。
2. 解释“会唱、待学、未收录、下一次 KTV”的关系。
3. 在 Pick 页面展示 KTV 剩余数量，区分跳过、唱完和本场耗尽。
4. 明确歌词跟唱是计时歌词能力，不包含音频播放、录音或音准评分。

## 第二轮结论

- 高频用户：页面内连续 Pick 可以运行，但刷新恢复、跳过反馈以及“唱完已保存、下一首失败”的状态需要拆分。
- 第一次使用用户：登录后直接进入 Pick，空会唱曲库缺少明确的选歌路径和状态解释。
- 弱网用户：静态外壳可缓存，API 仍依赖在线服务；缺少统一超时、离线横幅和可靠的操作结果反馈。

## 首批处理状态

- 已增加服务端活动场次恢复和 Pick 上下文接口，不创建额外场次。
- 已增加首次选歌引导、会唱/待学说明、KTV 剩余数量、KTV 耗尽分流和语言/曲风 Pick 筛选。
- 已将跳过、唱完、下一首加载拆分为明确状态，并让 Pick 重试复用原请求 ID。
- 已增加 15 秒请求超时、离线横幅、恢复网络刷新和初始连接重试。
- 已明确歌词能力只提供计时显示，不包含伴奏、录音和音准评分。
- 暂未实现离线写入队列、后台同步、API 数据持久化和冲突处理。
