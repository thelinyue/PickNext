services:
  picknext:
    build: .
    ports:
      - "5560:5560"
    environment:
      # 可在 .env 中设置 TZ，例如 TZ=UTC；未设置时使用东八区。
      TZ: ${TZ:-Asia/Shanghai}
    volumes:
      - ./data:/data
    restart: unless-stopped
