services:
  picknext:
    build: .
    ports:
      - "5560:5560"
    environment:
      JWT_SECRET: ${JWT_SECRET:?请在 .env 中设置至少32位的 JWT_SECRET}
    volumes:
      - picknext-data:/data
    restart: unless-stopped

volumes:
  picknext-data:
